apiVersion: v1
kind: Service
metadata:
  name: mcp-assessment
  labels:
    app: mcp-assessment
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: mcp-assessment
